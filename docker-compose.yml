version: '3.8'

services:
  phishing-detector:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./reports:/app/reports
      - ./models:/app/models
    environment:
      - FLASK_ENV=production
    restart: unless-stopped